<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>สถานะผู้ใช้</title>
    <link rel="stylesheet" href="/public/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="d-flex">
        <!-- Left Navbar -->
        <%- include("menu") %>

        <!-- Main Content -->
        <div class="container mt-5" id="main-content">
            <h2 class="text-center">สถานะของคุณ</h2>
            <% data.forEach(item => { %>
                <div class="card mb-3">
                    <div class="card-body">
                        <h5>ห้องหมายเลข: <%= item.room_id %></h5>
                        <p>สถานะห้อง: <%= item.room_status %></p>
                        <p>ค่าเช่าต่อเดือน: <%= item.price_per_month %> บาท</p>

                        <p>เลขที่สัญญา: <%= item.contract_ids %></p>
                        <p>วันที่เริ่ม: <%= item.start_date %></p>
                        <p>วันสิ้นสุด: <%= item.end_date %></p>
                        <p>สถานะสัญญา: <%= item.contract_status || '-' %></p>

                        <p>ยอดชำระ: <%= item.payment_amounts %> บาท</p>
                        <p>กำหนดชำระ: <%= item.payment_date || '-' %></p>
                        <p>สถานะการชำระ: <%= item.payment_status || '-' %></p>

                        <p>รายละเอียด: <%= item.repair_descriptions %></p>
                        <p>สถานะแจ้งซ่อม: <%= item.repair_status || '-' %></p>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>