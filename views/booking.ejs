<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ข้อมูลห้องพัก</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="d-flex">
        <!-- Left Navbar -->
        <%- include('menu') %>  <!-- เมนูข้าง -->

        <!-- Main Content -->
        <div class="container mt-5" id="main-content">
            <h1 class="text-center">ข้อมูลห้องพัก</h1>
            <p class="text-center">ดูข้อมูลห้องพักที่มีในปัจจุบัน</p>

            <div class="row">
                <% if (rooms.length === 0) { %>
                    <div class="col-12 text-center">
                        <p>ไม่พบข้อมูลห้องพัก</p>
                    </div>
                <% } else { %>
                    <% rooms.forEach(room => { %>
                        <div class="col-md-4 mb-4 d-flex justify-content-start">
                            <div class="card">
                                <div class="card-body">
                                    <h5 class="card-title">เลขห้อง: <%= room.room_id %></h5>
                                    <p><strong>ราคาเช่ารายเดือน:</strong> <%= room.price_per_month %> บาท</p>
                                    <p><strong>อินเทอร์เน็ต:</strong> 
                                        <% if (room.internet_conditioner) { %>
                                            ✅
                                        <% } else { %>
                                            ❌
                                        <% } %>
                                    </p>
                                    <p><strong>เครื่องปรับอากาศ:</strong> 
                                        <% if (room.air_conditioner) { %>
                                            ✅
                                        <% } else { %>
                                            ❌
                                        <% } %>
                                    </p>
                                    <p><strong>สถานะ:</strong> 
                                        <% if (room.status === "Available") { %>
                                            <span class="badge bg-success">ว่าง</span>
                                        <% } else { %>
                                            <span class="badge bg-danger">ไม่ว่าง</span>
                                        <% } %>
                                    </p>
                                </div>
                                <% if (room.status === "Available" ) { %>
                                    <a href="/booking-room/<%= room.room_id %>" class="btn btn-success">จองห้อง</a>
                                <% } else { %>
                                    <a href="#" class="btn btn-secondary disabled" aria-disabled="true" style="pointer-events: none;">
                                        🔒 ห้องไม่ว่าง
                                    </a>                                    
                                <% } %>
                            </div>
                        </div>
                    <% }) %>
                <% } %>
            </div>     
        </div>
    </div>     
</body>
</html>
